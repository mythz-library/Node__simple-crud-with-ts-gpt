{
  "name": "node__simple-crud-with-ts-gpt",
  "version": "1.0.0",
  "main": "server.ts",
  "scripts": {
    "dev": "npm run format && ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "tsc && node dist/server.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.3",
    "prettier": "^3.3.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.2"
  }
}
